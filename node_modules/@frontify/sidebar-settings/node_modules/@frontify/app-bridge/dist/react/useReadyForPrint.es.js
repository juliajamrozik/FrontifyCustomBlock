import { useState as s, useEffect as n } from "react";
const d = (e) => {
  const [r, o] = s(!1);
  return n(() => {
    const t = document.querySelector(`.block[data-block="${e.getBlockId()}"]`);
    if (!t) {
      console.error("Could not find block wrapper:", e.getBlockId());
      return;
    }
    t.setAttribute("data-ready", r.toString());
  }, [e, r]), { isReadyForPrint: r, setIsReadyForPrint: (t) => {
    o(t);
  } };
};
export {
  d as useReadyForPrint
};
//# sourceMappingURL=useReadyForPrint.es.js.map

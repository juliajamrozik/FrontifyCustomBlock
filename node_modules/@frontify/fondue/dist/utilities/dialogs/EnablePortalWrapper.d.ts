import { ReactElement, ReactNode } from 'react';
export declare const EnablePortalWrapper: {
    ({ enablePortal, children, }: {
        enablePortal?: boolean | undefined;
        children?: ReactNode;
    }): ReactElement;
    displayName: string;
};

import { MutableRefObject } from 'react';
export declare const DEFAULT_DROPDOWN_MAX_HEIGHT = "auto";
type DropdownAutoHeightProps = {
    isOpen: boolean;
    autoResize?: boolean;
    isDialog?: boolean;
};
export declare const getInnerOverlayHeight: (triggerRef: MutableRefObject<HTMLElement | null>) => string;
export declare const useDropdownAutoHeight: (triggerRef: MutableRefObject<HTMLElement | null>, { isOpen, autoResize }: DropdownAutoHeightProps) => {
    maxHeight: string;
};
export {};

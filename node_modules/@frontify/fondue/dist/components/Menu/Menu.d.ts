import { ReactNode, RefObject } from 'react';
interface Props {
    triggerRef?: RefObject<Element>;
    open?: boolean;
    children?: ReactNode | ReactNode[];
    onClose?: () => void;
    offset?: [number, number];
    'data-test-id'?: string;
}
export type MenuProps = Props;
export declare const Menu: {
    ({ triggerRef, children, open, offset, onClose, "data-test-id": dataTestId, }: MenuProps): JSX.Element;
    displayName: string;
};
export {};

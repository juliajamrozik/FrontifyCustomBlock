import { Dispatch, SetStateAction } from 'react';
import { TreeState } from '../types';
type AnnouncementArgs = {
    eventName: string;
    activeId: string;
    activeTitle: string;
    overId?: string;
    overTitle?: string;
    treeState: TreeState;
    currentPosition: Nullable<{
        overId: string;
        parentId: Nullable<string>;
    }>;
    setCurrentPosition: Dispatch<SetStateAction<Nullable<{
        overId: string;
        parentId: Nullable<string>;
    }>>>;
};
export declare const getMovementAnnouncement: ({ eventName, activeId, activeTitle, overId, overTitle, treeState, currentPosition, setCurrentPosition, }: AnnouncementArgs) => string | undefined;
export {};

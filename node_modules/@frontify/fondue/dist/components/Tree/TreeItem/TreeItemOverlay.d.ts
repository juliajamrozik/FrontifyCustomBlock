import { ReactNode } from 'react';
export type Overlay = {
    id: string;
    label?: string;
    level?: number;
    children: ReactNode;
    contentComponent?: ReactNode;
    isSelected?: boolean;
};
export declare const TreeItemOverlay: {
    ({ id, label, level, children, contentComponent, isSelected }: Overlay): JSX.Element;
    displayName: string;
};

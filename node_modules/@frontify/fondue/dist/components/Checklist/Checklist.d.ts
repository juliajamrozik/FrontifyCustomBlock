/// <reference types="react" />
import { CheckboxProps } from '../Checkbox/Checkbox';
export declare enum ChecklistDirection {
    Horizontal = "Horizontal",
    Vertical = "Vertical"
}
export type Columns = 1 | 2 | 3 | 4;
export type CheckboxValue = Omit<CheckboxProps, 'onChange' | 'groupInputProps' | 'value'> & {
    value: string;
};
export type ChecklistBase = {
    checkboxes: CheckboxValue[];
    setActiveValues: (value: string[]) => void;
    activeValues?: string[];
    ariaLabel?: string;
    'data-test-id'?: string;
};
export type ChecklistProps = ChecklistBase & {
    direction: ChecklistDirection.Vertical | ChecklistDirection.Horizontal;
    columns?: Columns;
};
export declare const Checklist: {
    ({ checkboxes, setActiveValues, ariaLabel, activeValues, direction, "data-test-id": dataTestId, ...props }: ChecklistProps): JSX.Element;
    displayName: string;
};

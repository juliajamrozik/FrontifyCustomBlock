import React from 'react';
import { DimensionUnity, SpacingValue } from '../../utilities/dimensions';
import { ContainerHTMLElement } from 'src/types/elements';
export type GridProps = {
    column?: number;
    spacingX?: SpacingValue;
    spacingY?: SpacingValue;
    children?: React.ReactNode;
    width?: `${number}${DimensionUnity}`;
    rowHeight?: `${number}${DimensionUnity}` | 'auto';
    padding?: SpacingValue;
    margin?: SpacingValue;
    bg?: string;
    color?: string;
    'data-test-id'?: string;
    as?: ContainerHTMLElement;
};
export declare const GRID_TEST_ID = "fondue-grid";
export declare const Grid: {
    ({ column, spacingX, spacingY, children, "data-test-id": dataTestId, width, rowHeight, bg, color, margin, padding, as: ContainerElement, }: GridProps): JSX.Element;
    displayName: string;
};

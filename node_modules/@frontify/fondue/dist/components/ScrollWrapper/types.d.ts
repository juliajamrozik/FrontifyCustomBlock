import { ReactElement } from 'react';
export declare enum ScrollWrapperDirection {
    Both = "Both",
    Horizontal = "Horizontal",
    Vertical = "Vertical"
}
export declare const scrollWrapperDirections: Record<ScrollWrapperDirection, string>;
export type ScrollWrapperProps = {
    direction?: ScrollWrapperDirection;
    children?: ReactElement | ReactElement[];
    'data-test-id'?: string;
    tabindex?: number;
    scrollShadows?: boolean;
};

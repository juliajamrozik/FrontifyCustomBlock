import { Position } from '../EditorPositioningWrapper';
import { Buttons, Plugin, ToolbarButtons } from '../Plugins/types';
export declare class ToolbarPositionWithButtons implements ToolbarButtons {
    private toolbarButtons;
    createGroupOfButtons(plugins: Plugin[]): void;
    at(position: Position): Buttons;
}

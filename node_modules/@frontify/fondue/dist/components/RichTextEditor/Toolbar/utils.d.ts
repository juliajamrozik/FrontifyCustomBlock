import { Button, Buttons } from '../Plugins/types';
import { ButtonGroupWidths, ButtonGroupsPerRow } from './types';
export declare const getButtonGroupWidths: (buttons: Buttons) => {
    group: Button[];
    buttonGroupWidth: number;
    index: number;
}[];
export declare const getButtonGroupsPerRow: (editorWidth: number, buttonGroupWidths: ButtonGroupWidths) => ButtonGroupsPerRow;

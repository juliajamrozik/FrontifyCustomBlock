import { PlateEditor } from '@udecode/plate';
import { Dispatch, HTMLAttributes, SetStateAction } from 'react';
type UseTextStyleDropdownReturn<T, P> = {
    state: {
        isOpen: boolean;
        toggle: () => void;
        editor: PlateEditor;
    };
    key: string;
    dropdownProps: HTMLAttributes<P>;
    triggerRef: Dispatch<SetStateAction<T | null>>;
    dropdownRef: Dispatch<SetStateAction<P | null>>;
};
export declare const useTextStyleDropdown: <T extends HTMLElement, P extends HTMLElement>(editorId?: string) => UseTextStyleDropdownReturn<T, P>;
export {};

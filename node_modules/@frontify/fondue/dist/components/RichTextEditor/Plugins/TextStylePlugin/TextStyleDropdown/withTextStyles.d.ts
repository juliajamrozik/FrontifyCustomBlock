import React, { ComponentType } from 'react';
import { Plugin } from '../../Plugin';
export declare const withTextStyles: <T extends Record<string, unknown>>(Component: React.ComponentType<T>, textStyles: Plugin[]) => (props: T) => JSX.Element;

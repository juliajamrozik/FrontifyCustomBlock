/// <reference types="react" />
import { DraggableItem } from '../../utilities/dnd';
import { CollisionPosition } from '..';
export type OnDropCallback<T> = (targetItem: DraggableItem<T>, sourceItem: DraggableItem<T>, position: CollisionPosition, direction?: 'up' | 'down') => void;
export type DropZoneData<T> = {
    targetItem: DraggableItem<T>;
    position: CollisionPosition;
};
export type DropZoneProps<T> = {
    data: DropZoneData<T>;
    onDrop?: OnDropCallback<T>;
    accept: string | string[];
    children?: JSX.Element;
    'data-test-id'?: string;
};
export declare const DropZone: {
    <T extends object>({ data, onDrop, accept, children, "data-test-id": dataTestId, }: DropZoneProps<T>): JSX.Element;
    displayName: string;
};

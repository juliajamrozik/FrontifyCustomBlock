import { ReactElement } from 'react';
import { SearchResultOptionProps } from './types';
export declare const SearchResultOption: ({ item, state, keyItemRecord, machineService, }: SearchResultOptionProps) => ReactElement;

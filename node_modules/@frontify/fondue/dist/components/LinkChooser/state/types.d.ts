export declare enum LinkChooserState {
    Idle = "idle",
    Focused = "focused"
}
export declare enum DropdownState {
    Default = "default",
    ExtraSection = "extra-section"
}
export declare enum SectionState {
    Loaded = "loaded",
    Typing = "typing",
    Fetching = "fetching",
    Error = "error"
}

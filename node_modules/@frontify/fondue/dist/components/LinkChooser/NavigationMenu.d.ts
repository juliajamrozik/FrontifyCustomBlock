import { ReactElement } from 'react';
import { NavigationMenuItemProps, NavigationMenuProps } from './types';
export declare const NavigationMenu: ({ machineService, state }: NavigationMenuProps) => Nullable<ReactElement>;
export declare const NavigationMenuItem: ({ section: { id, title }, onPress, state, direction, }: NavigationMenuItemProps) => ReactElement;

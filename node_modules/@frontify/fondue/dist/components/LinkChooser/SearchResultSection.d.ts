import { ReactElement } from 'react';
import { SearchResultSectionProps } from './types';
export declare const SearchResultSection: ({ heading, state, keyItemRecord, machineService, }: SearchResultSectionProps) => ReactElement;

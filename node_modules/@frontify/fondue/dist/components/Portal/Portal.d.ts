import { ReactNode } from 'react';
export type PortalProps = {
    children?: ReactNode;
    container?: HTMLElement;
};
export declare const Portal: {
    ({ container, children }: PortalProps): import("react").ReactPortal;
    displayName: string;
};

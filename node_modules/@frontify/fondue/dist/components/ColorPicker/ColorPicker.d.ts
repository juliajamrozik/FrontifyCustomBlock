/// <reference types="react" />
import { Color, ColorFormat, Palette } from '../../types/colors';
export type ColorPickerProps = {
    palettes?: Palette[];
    currentColor: Color;
    currentFormat: ColorFormat;
    setFormat: (id: ColorFormat) => void;
    onSelect: (color: Color) => void;
    showPreview?: boolean;
    allowCustomColor?: boolean;
};
export declare const ColorPicker: {
    ({ currentColor, palettes, onSelect, setFormat, showPreview, currentFormat, allowCustomColor, }: ColorPickerProps): JSX.Element;
    displayName: string;
};

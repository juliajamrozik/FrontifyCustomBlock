import { MenuItemProps } from '../../MenuItem/MenuItem';
import { AriaListBoxOptions } from '@react-aria/listbox';
import { ListState } from '@react-stately/list';
import { ReactElement } from 'react';
export type MenuItemType = Omit<MenuItemProps, 'switchComponent'> & {
    id: string | number;
    link?: string;
};
export type MenuBlock = {
    id: string;
    menuItems: MenuItemType[];
    ariaLabel?: string;
};
export type SelectMenuProps = {
    menuBlocks: MenuBlock[];
    ariaProps: AriaListBoxOptions<any>;
    state: ListState<any>;
    ariaLabel?: string;
    scrollable?: boolean;
};
export declare const SelectMenu: {
    ({ menuBlocks, ariaProps, state, ariaLabel, scrollable, }: SelectMenuProps): ReactElement<SelectMenuProps>;
    displayName: string;
};

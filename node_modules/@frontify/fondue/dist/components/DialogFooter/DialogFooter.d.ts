import { ReactElement } from 'react';
import { DialogFooterProps } from '../../types/dialog';
export declare const DialogFooter: {
    ({ actionButtons, children, backButton, padding, "data-test-id": dataTestId, }: DialogFooterProps): ReactElement;
    displayName: string;
};

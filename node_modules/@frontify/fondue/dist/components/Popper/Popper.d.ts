import React, { ReactElement, ReactNode } from 'react';
import { PopperProps } from '../Popper/types';
export declare const Popper: {
    ({ children, open, placement, offset, flip, enablePortal, zIndex, }: PopperProps): JSX.Element;
    displayName: string;
    Reference: {
        ({ children }: {
            children: ReactElement;
        }): React.ReactElement<any, string | React.JSXElementConstructor<any>>;
        displayName: string;
    };
    Content: {
        ({ children }: {
            children?: ReactNode;
        }): Nullable<ReactElement>;
        displayName: string;
    };
};

import { DimensionUnity, SpacingValue } from '../../utilities/dimensions';
import { ReactNode } from 'react';
import { ContainerHTMLElement } from 'src/types/elements';
export type ContainerProps = {
    children?: ReactNode | ReactNode[];
    minWidth?: `${number}${DimensionUnity}`;
    maxWidth?: `${number}${DimensionUnity}`;
    minHeight?: `${number}${DimensionUnity}`;
    maxHeight?: `${number}${DimensionUnity}`;
    padding?: SpacingValue;
    margin?: SpacingValue;
    bg?: string;
    color?: string;
    'data-test-id'?: string;
    as?: ContainerHTMLElement;
};
export declare const CONTAINER_TEST_ID = "fondue-container";
export declare const Container: {
    ({ children, "data-test-id": dataTestId, minWidth, maxWidth, maxHeight, minHeight, bg, color, margin, padding, as: ContainerElement, }: ContainerProps): JSX.Element;
    displayName: string;
};

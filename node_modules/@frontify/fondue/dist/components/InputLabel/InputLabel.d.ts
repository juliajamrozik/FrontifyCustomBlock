import { TooltipProps } from '../Tooltip/Tooltip';
import { TooltipIconProps } from '../TooltipIcon/TooltipIcon';
import { ReactElement, ReactNode } from 'react';
export type InputLabelTooltipProps = (TooltipProps & Pick<TooltipIconProps, 'triggerStyle' | 'triggerIcon'>) | (TooltipProps & Pick<TooltipIconProps, 'triggerStyle' | 'triggerIcon'>)[];
export type InputLabelProps = {
    htmlFor: string;
    required?: boolean;
    disabled?: boolean;
    clickable?: boolean;
    tooltip?: InputLabelTooltipProps;
    bold?: boolean;
    children?: ReactNode;
    'data-test-id'?: string;
};
export declare const InputLabel: {
    ({ children, htmlFor, required, disabled, clickable, tooltip, bold, "data-test-id": dataTestId, }: InputLabelProps): ReactElement;
    displayName: string;
};

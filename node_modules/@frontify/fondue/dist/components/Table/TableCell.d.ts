import { Key } from 'react';
export declare enum TableCellType {
    Default = "Default",
    Checkbox = "Checkbox"
}
export type TableCellProps = {
    cell: any;
    selectionMode: string;
    type?: TableCellType;
    isChecked?: boolean;
    selectedRows: Key[];
    setSelectedRows?: (ids?: Key[]) => void;
};
export declare const TableCell: {
    ({ cell, selectionMode, type, isChecked, selectedRows, setSelectedRows, }: TableCellProps): JSX.Element;
    displayName: string;
};

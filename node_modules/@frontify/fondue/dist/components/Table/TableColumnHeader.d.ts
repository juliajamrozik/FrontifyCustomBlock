import { Key } from 'react';
import { SortDirection } from '..';
export declare enum TableColumnHeaderType {
    Default = "Default",
    SelectAll = "SelectAll"
}
export type TableColumnHeaderProps = {
    column: any;
    type?: TableColumnHeaderType;
    rowIds: Key[];
    sortDirection?: SortDirection;
    selectionMode: string;
    isColumnSorted?: boolean;
    handleSortChange: (column: string, direction?: SortDirection) => void;
    setSelectedRows?: (ids?: Key[]) => void;
};
export declare const TableColumnHeader: {
    ({ column, type, rowIds, sortDirection, selectionMode, isColumnSorted, handleSortChange, setSelectedRows, }: TableColumnHeaderProps): JSX.Element;
    displayName: string;
};

import { Key, ReactNode } from 'react';
export declare enum SelectionMode {
    NoSelect = "none",
    SingleSelect = "single",
    MultiSelect = "multiple"
}
export type Cell = {
    sortId: string | number;
    value: ReactNode;
    ariaLabel?: string;
};
export type Column = {
    name: string;
    key: string;
    sortable?: boolean;
};
export type Row = {
    key: Key;
    cells: Record<string, Cell>;
    actionElements?: ReactNode;
};
export type TableProps = {
    columns: Column[];
    rows: Row[];
    onSelectionChange?: (ids?: Key[]) => void;
    onSortChange?: (column: string, direction?: SortDirection) => void;
    selectionMode?: SelectionMode;
    selectedRowIds?: Key[];
    ariaLabel?: string;
    children?: ReactNode;
};
export declare enum SortDirection {
    Ascending = "ascending",
    Descending = "descending"
}
export declare const Table: {
    ({ columns, rows, onSelectionChange, selectionMode, onSortChange: onSort, selectedRowIds, ariaLabel, }: TableProps): JSX.Element;
    displayName: string;
};

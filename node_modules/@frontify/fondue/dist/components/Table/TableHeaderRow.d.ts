import { ReactNode } from 'react';
type TableHeaderRowProps = {
    children: ReactNode;
};
export declare const TableHeaderRow: {
    ({ children }: TableHeaderRowProps): JSX.Element;
    displayName: string;
};
export {};

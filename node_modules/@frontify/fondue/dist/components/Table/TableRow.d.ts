import { ReactNode } from 'react';
export type TableRowProps = {
    isSelected?: boolean;
    children: ReactNode;
};
export declare const TableRow: {
    ({ isSelected, children }: TableRowProps): JSX.Element;
    displayName: string;
};
